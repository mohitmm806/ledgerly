# The backend API base URL. Read at BUILD time by Vite, so it must be set
# before `npm run build` (locally, or in the Vercel project settings).
# Local dev default is http://localhost:8000, so you can leave this unset locally.
VITE_API_BASE=https://your-backend-url.onrender.com
